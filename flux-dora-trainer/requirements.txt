# ============================================================
# FLUX LoRA Trainer — Pinned Requirements
# Sourced from: replicate/flux-fine-tuner cog.yaml
# Python 3.10.14 | CUDA 12.4 | torch 2.4.1
# ============================================================

# --- Core PyTorch (install separately with correct CUDA index) ---
# pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu124
torch==2.4.1
torchvision==0.19.1

# --- FLUX / diffusion stack (pinned to match Replicate) ---
safetensors==0.4.4
diffusers==0.30.0
transformers==4.44.0
accelerate==0.33.0
peft==0.13.0

# diffusers fork with FLUX inpaint support (must come AFTER diffusers==0.30.0)
# pip install git+https://github.com/Gothos/diffusers.git@flux-inpaint

# --- LoRA / network ---
lycoris-lora==1.8.3

# --- Quantization (for < 40 GB VRAM) ---
bitsandbytes==0.43.3
optimum-quanto==0.2.4

# --- Optimizers ---
prodigyopt==1.0
lion-pytorch==0.2.2

# --- Image / vision ---
pillow==10.4.0
opencv-python==4.10.0.84
scikit-image==0.24.0
kornia==0.7.3
albumentations==1.4.3
lpips==0.1.4
open_clip_torch==2.26.1
timm==1.0.8

# --- AI toolkit deps ---
einops==0.8.0
omegaconf==2.3.0
pyyaml==6.0.1
oyaml==1.0
toml==0.10.2
flatten_json==0.1.14
tensorboard==2.17.0
controlnet_aux==0.0.7

# --- HuggingFace ---
huggingface-hub==0.24.5
hf_transfer==0.1.8
sentencepiece==0.2.0
tokenizers==0.19

# --- LLaVA captioner deps ---
deepspeed==0.9.5
einops-exts==0.0.4
shortuuid==1.0.11
markdown2==2.4.10
wavedrom==2.0.3.post3
Pygments==2.16.1

# LLaVA itself (install from source)
# pip install git+https://github.com/haotian-liu/LLaVA.git

# --- ai-toolkit extras (not in cog.yaml but required by ai-toolkit internals) ---
k-diffusion
invisible-watermark
pytorch_fid
python-dotenv

# --- Logging ---
wandb==0.17.8

# --- Misc ---
numpy==1.26.0
pydantic==1.10.17
httpx==0.24.0
