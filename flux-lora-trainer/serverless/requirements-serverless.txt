# Serverless-layer deps only. Base image already has torch/diffusers/ai-toolkit.
# Pin everything — we're chasing parity, not freshness.
runpod==1.7.2
boto3==1.35.20
botocore==1.35.20
requests==2.32.3
jsonschema==4.23.0
sentry-sdk==2.14.0

# Transitive pins: runpod 1.7.2 declares `fastapi[all]>=0.94.0` which pulls
# fastapi-cli[standard] with a loose graph. Without these lower bounds pip's
# 2020 resolver explores hundreds of version combinations and eventually aborts
# with `resolution-too-deep` after ~40 minutes. Pinning the biggest offenders
# to known-good versions collapses the search space.
fastapi==0.115.0
uvicorn[standard]==0.30.6
fastapi-cli==0.0.5
email-validator==2.2.0
dnspython==2.7.0
sniffio==1.3.1
exceptiongroup==1.2.2
python-multipart==0.0.12
orjson==3.10.7
ujson==5.10.0
